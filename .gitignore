/bin/

# env / secrets
.env
.env.*
!.env.example
!.env.template

# Local / secret prompt copy (keep template or docs in repo; see .env.example)
prompts/translate_context.txt

# credentials / keys / certs
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.jks
*.kubeconfig

# local runtime artifacts
*.log
*.pid

# local docker overrides
docker-compose.override.yml

# OS / editor
.DS_Store
.idea/
.vscode/

